# Azure Functions
FUNCTIONS_WORKER_RUNTIME=python

# Azure Storage
# Configure these values only in your local environment.
# Never commit real credentials.
AzureWebJobsStorage=
AZURE_STORAGE_CONNECTION_STRING=
